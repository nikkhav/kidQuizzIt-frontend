import { childCategory } from "./childCategory";

export interface fairyData {
  category: childCategory;
  category_id: number;
  description: string;
  id: number;
  image: string;
  title: string;
}
