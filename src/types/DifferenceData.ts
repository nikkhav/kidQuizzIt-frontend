import { childCategory } from "./childCategory";

export interface differenceData {
  category: childCategory;
  category_id: number;
  id: number;
  title: string;
  description: string;
  image: string;
}
